# MongoDB Connection
MONGODB_URI=mongodb://localhost:27017/charity

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server Configuration
PORT=3000
CORS_ORIGIN=*

# ═══════════════════════════════════════════════
# BLOCKCHAIN CONFIGURATION
# ═══════════════════════════════════════════════

# Chế độ hoạt động:
#   mock       → in-memory, không cần Fabric (dùng cho dev/test)
#   production → kết nối Hyperledger Fabric thật
BLOCKCHAIN_MODE=mock

# ─── Hyperledger Fabric (chỉ cần khi BLOCKCHAIN_MODE=production) ────────────

# Tên channel Fabric (phải trùng với channel được tạo khi dựng network)
FABRIC_CHANNEL_NAME=mychannel

# Tên chaincode đã deploy
FABRIC_CHAINCODE_NAME=charity-chaincode

# Đường dẫn đến file wallet (chứa identity của appUser sau khi enroll)
FABRIC_WALLET_PATH=./wallet

# Đường dẫn đến connection profile JSON
FABRIC_CONNECTION_PROFILE=./fabric/connection-profile.json

# Identity dùng để submit transactions (phải tồn tại trong wallet)
FABRIC_USER_ID=appUser

# MSP ID của organization (thường là Org1MSP với test-network)
FABRIC_MSP_ID=Org1MSP

# Thông tin Fabric CA để enroll users (dùng khi chạy wallet-helper.ts)
FABRIC_CA_NAME=ca.org1.example.com
FABRIC_ADMIN_USER=admin
FABRIC_ADMIN_PASS=adminpw
